wsgidav>=4.3
cheroot>=10.0
telethon>=1.44,<2
# Native AES-IGE. Without it Telethon falls back to a pure-Python implementation
# that decrypts at ~0.15 MiB/s and caps every download well below the link speed;
# the wheel lifts a single connection to several MiB/s. Not optional in practice.
cryptg>=0.4
requests>=2.31

# Decodes the images this bridge uploads, to build the preview and dimensions
# Telegram will not infer for a force_document upload. Without it uploads still
# work, they just arrive with no thumbnail -- which sends Explorer off to read
# every whole file to draw an icon (see the upload-preview note in CLAUDE.md).
Pillow>=10.0
